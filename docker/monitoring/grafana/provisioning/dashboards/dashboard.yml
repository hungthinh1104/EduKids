apiVersion: 1

providers:
  - name: 'EduKids Dashboards'
    orgId: 1
    folder: 'EduKids'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
